function applyFilters() {
        const selectedSortOption = document.getElementById("sort").value;
        console.log(selectedSortOption);
        const selectedProducerOption = document.getElementById("sort").value;
}